import React, { VFC } from "react";

export const NotDetected: VFC = () => {
  return (
    <div style={{ padding: "1rem", width: "200px" }}>
      <div>この拡張機能は Google Meet でのみご利用になれます。</div>
    </div>
  );
};
